[package]
name = "py-dtex"
version = "0.1.0"
edition = "2018"

[dependencies]
pyo3 = { version = "0.20.0", features = ["extension-module"] }
dtex = { path = ".." }

[lib]
name = "dtex"
crate-type = ["cdylib"]
